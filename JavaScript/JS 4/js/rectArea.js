let ax = Number(prompt('A(x): '));
let ay = Number(prompt('A(y): '));
let bx = Number(prompt('B(x): '));
let by = Number(prompt('B(y): '));

let area = (bx - ax) * (by - ay);

alert(`Area of rectangle is: ${area}`);
